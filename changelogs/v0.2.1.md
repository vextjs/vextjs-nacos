 # v0.2.1

- 修复 `vextjs-nacos` 在 CJS / bootstrap 加载链路下的包导出兼容问题
- 补齐 `exports.require` 与 `main` CJS 入口
- 构建同时产出 ESM `dist/index.js` 与 CJS `dist/index.cjs`
- 保证 `createNacosBootstrapProvider()` 等最新导出在发布包中可被 `import` / `require` 正常访问

