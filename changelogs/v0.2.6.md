# v0.2.6

Released: 2026-06-09

## Changed

- Pinned direct runtime and development dependencies in `package.json` to exact versions already resolved by `package-lock.json`.
- Kept the public `vextjs` peer dependency compatibility range unchanged.

## Verification

- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm pack --dry-run --json`
